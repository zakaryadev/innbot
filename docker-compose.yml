version: '3.8'

services:
  telegram_bot:
    build: .
    container_name: inn_telegram_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Mount the database file so it persists and can be updated from the host
      - ./data.db:/app/data.db
